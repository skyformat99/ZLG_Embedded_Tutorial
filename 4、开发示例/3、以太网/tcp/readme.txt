使用方法：

1、在开发套件上运行tcp_server程序
	./tcp_server

2、在ubuntu系统上运行tcp_client程序
	./tcp_client 192.168.1.84 （这个是开发套件的IP地址，需要根据实际情况填写）

3、根据开发套件输出的提示信息，在终端中输入一个字符串，按回车后可以看到ubuntu打印
   出了所接收到的字符串

4、按Ctrl-c退出程序